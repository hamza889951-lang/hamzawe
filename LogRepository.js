/**
 * ═══════════════════════════════════════
 * CONTRACT — LogRepository
 * ═══════════════════════════════════════
 * يضمن:
 *   - كتابة سجل عملية أعمال واحدة في SYSTEM_LOG.
 * لا يضمن:
 *   - أي قراءة، بحث، تحليل، أو حذف.
 *
 * قيد دائم (Append Only):
 * هذا الملف يكتب فقط ولا شيء غير ذلك. ممنوع إضافة أي دالة قراءة أو بحث
 * أو حذف إليه مستقبلاً — أي حاجة لتحليل السجل تُبنى في أداة منفصلة تقرأ
 * من الورقة، لا تُضاف هنا.
 */
const LogRepository = {
  write(entry) {
    GoogleSheets.appendRow(Config.VOCABULARY.SHEETS.SYSTEM_LOG, entry);
  }
};